CREATE TABLE beer_presentation(
	id SERIAL NOT NULL,
	presentation VARCHAR(20)
);

CREATE TABLE brewing_material(
	id SERIAL NOT NULL,
	material VARCHAR(30)
);

CREATE TABLE brewing_type(
	id SERIAL NOT NULL,
	types VARCHAR(30)
);

